/* Schlumberger Private
Copyright 2018 Schlumberger.  All rights reserved in Schlumberger
authored and generated code (including the selection and arrangement of
the source code base regardless of the authorship of individual files),
but not including any copyright interest(s) owned by a third party
related to source code or object code authored or generated by
non-Schlumberger personnel.

This source code includes Schlumberger confidential and/or proprietary
information and may include Schlumberger trade secrets. Any use,
disclosure and/or reproduction is prohibited unless authorized in
writing. */
import { Routes, RouterModule } from '@angular/router';
import { ReleaseNotesComponent } from 'src/app/base/release-notes/release-notes.component';
import { RouteConfigPaths } from 'src/Constants/helpers';
import { FMPAuthGuard } from './base/helpers/Guard/fmpauth.guard';

const appRouting: Routes = [
  //#region Default Routes
  {
    path: '',
    redirectTo: RouteConfigPaths.Home.path,
    pathMatch: 'full',
  },
  //#endregion
  //#region Page Routes
  {
    path: RouteConfigPaths.ReleaseNotes.name,
    component: ReleaseNotesComponent,
    canActivate: [FMPAuthGuard],
    canLoad: [FMPAuthGuard],
  },
  //#endregion
  //#region Unknown Route Handler.
  {
    path: '**',
    redirectTo: RouteConfigPaths.NotFound.path,
  },
  //#endregion
];

export const appRoutes = RouterModule.forRoot(appRouting, {
  useHash: true,
  initialNavigation: false,
  onSameUrlNavigation: 'ignore',
  enableTracing: false,
});

/* Schlumberger Private
Copyright 2018 Schlumberger.  All rights reserved in Schlumberger
authored and generated code (including the selection and arrangement of
the source code base regardless of the authorship of individual files),
but not including any copyright interest(s) owned by a third party
related to source code or object code authored or generated by
non-Schlumberger personnel.

This source code includes Schlumberger confidential and/or proprietary
information and may include Schlumberger trade secrets. Any use,
disclosure and/or reproduction is prohibited unless authorized in
writing. */
import { async, ComponentFixture, TestBed } from '@angular/core/testing';
import { UserprefrenceComponent } from './userprefrence.component';
import { Component } from '@angular/core';
import { PopoverModule, BsModalService, ComponentLoaderFactory, PositioningService } from 'ngx-bootstrap';
import { HttpClientModule } from '@angular/common/http';
import { TranslateModule } from '@ngx-translate/core';
import { DropDownsModule } from '@progress/kendo-angular-dropdowns';
import { FormsModule } from '@angular/forms';
import { UserprofileService } from 'src/app/base/userprofile/userprofile.service';


@Component({
  selector: 'app-fmp-contexthelp',
  template: '',
})
// tslint:disable-next-line:max-classes-per-file
export class ContexthelpMockComponent {
  
}

xdescribe('UserprefrenceComponent', () => {
  let component: UserprefrenceComponent;
  let fixture: ComponentFixture<UserprefrenceComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      imports: [
        PopoverModule,
        HttpClientModule,
        TranslateModule.forRoot(),
        DropDownsModule,
        FormsModule
      ],
      declarations: [ UserprefrenceComponent, ContexthelpMockComponent ],
      providers: [
        UserprofileService,
        BsModalService,
        ComponentLoaderFactory,
        PositioningService
      ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(UserprefrenceComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

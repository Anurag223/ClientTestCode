import { RepositionELementDirective } from './reposition-element.directive';

describe('RepositionELementDirective', () => {
  it('should create an instance', () => {
    const directive = new RepositionELementDirective();
    expect(directive).toBeTruthy();
  });
});

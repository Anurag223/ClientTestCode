import { ContextHelpElementDirective } from './context-help-element.directive';

describe('ContextHelpElementDirective', () => {
  it('should create an instance', () => {
    const directive = new ContextHelpElementDirective();
    expect(directive).toBeTruthy();
  });
});

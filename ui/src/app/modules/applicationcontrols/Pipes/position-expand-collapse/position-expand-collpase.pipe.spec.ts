import { PositionExpandCollpasePipe } from './position-expand-collpase.pipe';

xdescribe('PositionExpandCollpasePipe', () => {
  it('create an instance', () => {
    // const pipe = new PositionExpandCollpasePipe();
    expect(PositionExpandCollpasePipe).toBeTruthy();
  });
});

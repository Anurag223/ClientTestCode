/* Schlumberger Private
Copyright 2018 Schlumberger.  All rights reserved in Schlumberger
authored and generated code (including the selection and arrangement of
the source code base regardless of the authorship of individual files),
but not including any copyright interest(s) owned by a third party
related to source code or object code authored or generated by
non-Schlumberger personnel.

This source code includes Schlumberger confidential and/or proprietary
information and may include Schlumberger trade secrets. Any use,
disclosure and/or reproduction is prohibited unless authorized in
writing. */
import { Injectable } from '@angular/core';
import { TranslateService } from '@ngx-translate/core';
import { HttpClient } from '@angular/common/http';

export interface LangConfig {
  default_lang: string;
  language_array: string[];
}
@Injectable({
  providedIn: 'root',
})
export class AppTranslateService {
  private langSettings: LangConfig;
  constructor(private translate: TranslateService, private http: HttpClient) {
    // const langURL = '../assets/i18n/language.json';
    // this.http.get<LangConfig>(langURL).subscribe(res => {
    //     this.langSettings = res;
    //     translate.addLangs(this.langSettings.language_array);
    //     translate.setDefaultLang(this.langSettings.default_lang);

    //     let page_language = this.langSettings.default_lang;
    //     const browserLang = translate.getBrowserLang();
    //     for (let i = 0; i < this.langSettings.language_array.length; i++) {

    //         if (browserLang === this.langSettings.language_array[i]) {
    //             page_language = browserLang;
    //         }
    //     }
    //     translate.use(page_language);
    // });

    const data = {
      default_lang: 'en',
      language_array: ['en', 'fr'],
    };

    translate.addLangs(data.language_array);
    translate.setDefaultLang(data.default_lang);

    let page_language = data.default_lang;

    const browserLang = translate.getBrowserLang();

    for (let i = 0; i < data.language_array.length; i++) {
      if (browserLang === data.language_array[i]) {
        page_language = browserLang;
      }
    }
    translate.use(page_language);
  }

  changeDefaultLanguage(lang) {
    this.translate.use(lang);
  }

  getCurrentLanguge(): string {
    return this.translate.currentLang;
  }
}

/* Schlumberger Private
Copyright 2018 Schlumberger.  All rights reserved in Schlumberger
authored and generated code (including the selection and arrangement of
the source code base regardless of the authorship of individual files),
but not including any copyright interest(s) owned by a third party
related to source code or object code authored or generated by
non-Schlumberger personnel.

This source code includes Schlumberger confidential and/or proprietary
information and may include Schlumberger trade secrets. Any use,
disclosure and/or reproduction is prohibited unless authorized in
writing. */
import { Component, ChangeDetectionStrategy } from '@angular/core';
import { LoaderService } from 'src/app/base/loader/loader.service';
import { BaseComponent } from 'src/app/base/component/base/base.component';
import { LoggerService } from 'src/app/base/service/logger.service';

@Component({
  selector: 'app-loader',
  templateUrl: './loader.component.html',
  styleUrls: ['./loader.component.css']
})
export class LoaderComponent extends BaseComponent {

  get LoadingText(): string {
    if (this._loaderService.LoadingText && this._loaderService.LoadingText.length > 0) {
      return this._loaderService.LoadingText;
    } else {
      return 'Loading...';
    }
  }

  constructor(private _loaderService: LoaderService, _loggerService: LoggerService) {
    super('LoaderComponent', _loggerService);
  }

  public loaderConfig = {
    overlayColor: 'rgba(51,51,51,0.2)',
    loaderSize: 'medium',
    loaderColor: '#136FD5',
    loaderType: 'line-scale'
  };
}

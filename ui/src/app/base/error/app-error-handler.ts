/* Schlumberger Private
Copyright 2018 Schlumberger.  All rights reserved in Schlumberger
authored and generated code (including the selection and arrangement of
the source code base regardless of the authorship of individual files),
but not including any copyright interest(s) owned by a third party
related to source code or object code authored or generated by
non-Schlumberger personnel.

This source code includes Schlumberger confidential and/or proprietary
information and may include Schlumberger trade secrets. Any use,
disclosure and/or reproduction is prohibited unless authorized in
writing. */
import { LoggerService, ErrorLog } from 'src/app/base/service/logger.service';
import { AppToasterService } from 'src/app/base/toaster/toaster.service';
import { FMPHelper } from 'src/Constants/helpers';
import { ErrorHandler } from '@angular/core';

export class AppErrorHandler implements ErrorHandler {

  static loggerService: LoggerService;
  static toasterService: AppToasterService;

  handleError(error) {
    if (!AppErrorHandler.loggerService) {
      AppErrorHandler.loggerService = FMPHelper.AngularInjector.get(LoggerService);
    }

    if (AppErrorHandler.loggerService) {
      AppErrorHandler.loggerService.WriteError('AppErrorHandler', 'An unhandled exception has ouccured.', error);
    } else if (console.error) {
      console.error(error);
    } else {
      console.log(error);
    }


    // if (!AppErrorHandler.toasterService) {
    //   AppErrorHandler.toasterService = FMPHelper.AngularInjector.get(AppToasterService);
    // }
    // if (AppErrorHandler.toasterService) {
    //   AppErrorHandler.toasterService.ShowError('An unhandled exception has ouccured.');
    // }
  }
}
